// paiement-aside.ts: removed — aside is now rendered server-side in PHP using HTML forms (no TS/JS required).
// If you need AJAX later, reintroduce a small JS file that posts with X-Requested-With: 'XMLHttpRequest'.

export {};
